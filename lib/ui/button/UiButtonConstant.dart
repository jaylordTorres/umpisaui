enum UiButtonColor { primary, secondary, success, warning, alert, none }
enum UiButtonPaddding { lg, md, sm, xs }
enum UiButtonMargin { lg, md, sm }
enum UiButtonShape { rounded, circular, squared }
enum UiButtonSize { lg, md, sm }
enum UiButtonVariant {
  // flat,
  // raised,
  outlined,
  contained,
  none,
}
